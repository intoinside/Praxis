---
description: Create a new intent
---

To execute this Praxis command, run:

// turbo
npx ts-node --loader ts-node/esm src/index.ts intent create "$description"

Once the command is executed, follow the prompt generated in the output to complete the intent document with the AI.
